# Admin (Vite + React + TypeScript)

Basic starter with React Router and a mock auth flow.

Quick start (zsh):

```bash
cd /Users/annakuprushyna/Documents/frontend/admin
npm install
npm run dev
```

Open http://localhost:5173

Files added:

- `src/` with app code
- `package.json`, `tsconfig.json`, `index.html`

Next steps: install deps and run the dev server. Adjust auth to your backend when ready.
